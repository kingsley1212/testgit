<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aaa.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".music.MusicAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".Main2Activity"></activity> -->
        <!-- <activity android:name=".Main3Activity"></activity> -->
        <!-- <activity android:name=".shiyi_yi.TimeActivity"></activity> -->
        <!-- <activity android:name=".tong_xl.mail_list_Activity"></activity> -->
        <!-- <activity android:name=".ToolBar.ToolBarActivity"></activity> -->
        <!--<activity android:name=".music.MusicAct"></activity>-->
    </application>

</manifest>